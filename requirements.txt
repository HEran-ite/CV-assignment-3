# Deep Learning for CV — Assignment 3 (LeNet-5 + NASNet)
# Python 3.10+ recommended. Install: pip install -r requirements.txt
torch>=2.0.0
torchvision>=0.15.0
timm>=1.0.0
pretrainedmodels>=0.7.4   # NASNet-A Mobile (Cadene's PyTorch port)
six                        # transitive dep used by pretrainedmodels submodules
